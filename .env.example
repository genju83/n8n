# DuckDNS 설정
# https://www.duckdns.org/ 에서 토큰을 발급받으세요
DUCKDNS_TOKEN=your-duckdns-token-here
DUCKDNS_SUBDOMAIN=your-subdomain

# 도메인 설정
# your-subdomain을 DuckDNS에서 생성한 실제 서브도메인으로 변경하세요
DOMAIN=your-subdomain.duckdns.org
N8N_HOST=your-subdomain.duckdns.org
N8N_PROTOCOL=https
WEBHOOK_URL=https://your-subdomain.duckdns.org

# 타임존 설정
GENERIC_TIMEZONE=Asia/Seoul

# n8n 이메일 설정 (선택사항)
# Resend SMTP 설정 (https://resend.com/)
# 1. Resend 가입 후 API Keys에서 API 키 발급
# 2. 아래 주석 제거하고 실제 값 입력

# N8N_EMAIL_MODE=smtp
# N8N_SMTP_HOST=smtp.resend.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=resend
# N8N_SMTP_PASS=re_123456789_YourResendAPIKey
# N8N_SMTP_SENDER=onboarding@resend.dev
#
# 참고:
# - N8N_SMTP_USER는 항상 "resend"로 고정
# - N8N_SMTP_PASS에 Resend API 키 입력 (re_로 시작)
# - 테스트용은 onboarding@resend.dev 사용 가능
# - 프로덕션은 본인 도메인 인증 후 사용 (예: noreply@yourdomain.com)
