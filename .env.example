# DuckDNS 설정
# https://www.duckdns.org/ 에서 토큰을 발급받으세요
DUCKDNS_TOKEN=your-duckdns-token-here
DUCKDNS_SUBDOMAIN=your-subdomain

# 도메인 설정
# your-subdomain을 DuckDNS에서 생성한 실제 서브도메인으로 변경하세요
DOMAIN=your-subdomain.duckdns.org
N8N_HOST=your-subdomain.duckdns.org
N8N_PROTOCOL=https
WEBHOOK_URL=https://your-subdomain.duckdns.org

# 타임존 설정
GENERIC_TIMEZONE=Asia/Seoul

# n8n 이메일 설정 (선택사항)
# Brevo (구 Sendinblue) SMTP 설정 (https://www.brevo.com/)
# 1. Brevo 가입 후 SMTP & API > SMTP Keys에서 SMTP 키 발급
# 2. Senders 메뉴에서 발신자 이메일 주소 추가 및 검증
# 3. 아래 주석 제거하고 실제 값 입력

# N8N_EMAIL_MODE=smtp
# N8N_SMTP_HOST=smtp-relay.brevo.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=your-email@example.com
# N8N_SMTP_PASS=your-brevo-smtp-key
# N8N_SMTP_SENDER=your-email@example.com
# N8N_SMTP_SSL=false
# N8N_SMTP_IGNORE_TLS=false
#
# 참고:
# - N8N_SMTP_USER: Brevo에 가입한 계정 이메일
# - N8N_SMTP_PASS: Brevo SMTP Key (비밀번호 아님!)
# - N8N_SMTP_SENDER: Brevo에서 검증한 발신자 이메일
# - PORT 587은 STARTTLS, PORT 465는 SSL 사용
# - 하루 300개 무료 발송 가능
